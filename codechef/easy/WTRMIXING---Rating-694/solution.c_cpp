#include<stdio.h>
int main()
{
    int t,a,b,x,y;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d%d%d%d",&a,&b,&x,&y);
        int z=a+x;
        int w=a-y;
        if(z>=b && w<=b)
        printf("YEs\n");
        else
        printf("No\n");
    }
}