#include <stdio.h>

int main() {
	// your code goes here
	int t,m,n,k,p;
	scanf("%d",&t);
	while(t--)
	{
	    scanf("%d%d%d%d",&m,&n,&k,&p);
	    if(m==0 && n==0 && k==0 && p==0)
	    printf("IN\n");
	    else
	    printf("OUT\n");
	}

}

