#include <stdio.h>
int main()
{
    int t,n,x;
    
    scanf("%d",&t);
    while(t--)
    {
        int count=0;
        scanf("%d",&n);
        while(n!=0)
        {
            x=n%10;
            n=n/10;
            count=count+x;
            
        }
        printf("%d\n",count);
    }
}