#include <stdio.h>

int main()
{
    int t,x;
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d",&x);
        if(x>=67 && x<=45000)
        printf("Yes \n");
        else
        printf("No \n");
    }
}

