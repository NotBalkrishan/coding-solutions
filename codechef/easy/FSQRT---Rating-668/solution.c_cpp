#include <stdio.h>

#include <math.h>

int main() {
    // your code goes here
    int t, x;
    scanf("%d", & t);
    while (t--)
    {
        scanf("%d", & x);
        float z=pow(x,0.5);
        printf("%.f\n",z);
    }

}