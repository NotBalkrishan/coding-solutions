#include <stdio.h>

int main() {
	// your code goes here
	int t,a,b,x,y;
	scanf("%d",&t);
	while(t--)
	{
	    int count=0;
	    scanf("%d%d%d%d",&x,&y,&a,&b);
	    if(x!=a && x!=b)
	    count++;
	    if(y!=a && y!=b)
	    count++;
	    
	    printf("%d\n", count);
	}

}

